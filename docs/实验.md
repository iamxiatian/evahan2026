# 实验数据

## Qwen2.5-VL-7B-Instruct 无微调情况下的指令效果

### OCR效果

指令：

```plaintext
提取图中的文字，以自然阅读顺序输出。
```

模型输出示例：

```plaintext
[INFO:swift] {'response': '陽殿弘璧天球莫喻輝赫癸卯冬十二月安西妃大宴興慶池賜', 'infer_request': {'messages': [{'role': 'user', 'content': [{'type': 'image_url', 'image_url': {'url': '<<<base64:data:image/jpg;base64,/9j/4AAQSkZJRgABAQAAAQABAAD/..>>>'}}, {'type': 'text', 'text': '提取图中的文字，以自然阅读顺序输出。'}]}], 'images': [], 'audios': [], 'videos': [], 'tools': None, 'objects': {}}, 'generation_config': GenerationConfig({'bos_token_id': 151643, 'do_sample': True, 'eos_token_id': [151645, 151643], 'max_new_tokens': 4096, 'pad_token_id': 151643, 'repetition_penalty': 1.05, 'return_dict_in_generate': True, 'stop_words': ['<|endoftext|>', '<|im_end|>'], 'temperature': 1e-06})}
```


缩放要求：

我想对图片处理，方便QwenVL2.5更准确的定位目标的检测位置。我统一设置一个924X1232的背景图片，背景颜色为白色到灰色之间的随机背景，原始图片如果长度和宽度都小于924X1232，再将其粘贴到左上角开始的位置上，得到一张新图片；如果跟背景图片相比有一定冗余空间，则继续从左上角偏移（x，y）个位置，粘贴到背景上，并记录随机偏移值（x，y），得到另一张图片。 

如果图片的长或宽超过了924、1232，则将原始图片缩放一定比例，使其能够放入到背景上，并略有空余，继续按上面的方式处理，得到两张新图片。
